name: bluepointliving
region: nyc

static_sites:
  - name: bluepointliving
    environment_slug: hugo
    github:
      repo: mjones129/BluepointLiving
      branch: master
      deploy_on_push: true
    source_dir: .
    output_dir: public/
