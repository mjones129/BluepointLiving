name: bluepointliving
region: nyc
static_sites:
- name: bluepointliving
  github:
    branch: master
    repo: mjones129/BluepointLiving
  build_command: hugo --minify --baseURL "${APP_URL}"
  output_dir: public
  source_dir: .